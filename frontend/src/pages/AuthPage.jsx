import { Box, LockKeyhole, Mail, UserRound } from 'lucide-react'
import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { register } from '../api/auth'
import { getErrorMessage } from '../api/client'
import { useAuth } from '../context/AuthContext'
export default function AuthPage({ mode }) { const isLogin = mode === 'login'; const navigate = useNavigate(); const { login } = useAuth(); const [form, setForm] = useState({ name: '', email: '', password: '', password_confirmation: '' }); const [loading, setLoading] = useState(false); const [error, setError] = useState(''); const change = e => setForm({ ...form, [e.target.name]: e.target.value }); const submit = async e => { e.preventDefault(); setError(''); if (!isLogin && form.password !== form.password_confirmation) return setError('Пароли не совпадают.'); setLoading(true); try { if (isLogin) { await login({ email: form.email, password: form.password }); navigate('/dashboard') } else { await register({ name: form.name, email: form.email, password: form.password }); navigate('/login') } } catch (err) { setError(getErrorMessage(err)) } finally { setLoading(false) } }; return <div className="auth-page"><section className="auth-promo"><div className="promo-brand"><Box/> Мой Склад</div><div><span className="eyebrow">УПРАВЛЯЙТЕ ЛЕГКО</span><h1>Ваш склад.<br/><em>Всё под контролем.</em></h1><p>Товары, поставки и продажи — в одном понятном сервисе.</p></div></section><main className="auth-form-wrap"><form className="auth-form" onSubmit={submit}><div className="mobile-brand"><Box/> Мой Склад</div><span className="eyebrow">{isLogin ? 'С ВОЗВРАЩЕНИЕМ' : 'НОВЫЙ АККАУНТ'}</span><h2>{isLogin ? 'Войдите в аккаунт' : 'Создайте аккаунт'}</h2><p>{isLogin ? 'Введите данные, чтобы продолжить работу.' : 'Начните управлять складом за пару минут.'}</p>{error && <div className="form-error">{error}</div>}{!isLogin && <label>Имя<div className="input"><UserRound size={18}/><input name="name" required value={form.name} onChange={change} placeholder="Ваше имя"/></div></label>}<label>Email<div className="input"><Mail size={18}/><input type="email" name="email" required value={form.email} onChange={change} placeholder="name@example.com"/></div></label><label>Пароль<div className="input"><LockKeyhole size={18}/><input type="password" name="password" required minLength="6" value={form.password} onChange={change} placeholder="Минимум 6 символов"/></div></label>{!isLogin && <label>Подтвердите пароль<div className="input"><LockKeyhole size={18}/><input type="password" name="password_confirmation" required value={form.password_confirmation} onChange={change} placeholder="Повторите пароль"/></div></label>}<button className="primary full" disabled={loading}>{loading ? 'Подождите…' : isLogin ? 'Войти' : 'Зарегистрироваться'}</button><p className="auth-switch">{isLogin ? 'Нет аккаунта?' : 'Уже есть аккаунт?'} <Link to={isLogin ? '/register' : '/login'}>{isLogin ? 'Зарегистрироваться' : 'Войти'}</Link></p></form></main></div> }
